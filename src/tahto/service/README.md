# Service boundary

TAHTO-6 implements inert application service descriptors and durable job state here. Worker implementations remain in application repositories and are pinned by immutable package or binary digest.
